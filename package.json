{
  "name": "quip-network-node-runner",
  "version": "0.1.0-alpha.1",
  "description": "Quip Network Node Runner - Desktop Application",
  "license": "AGPL-3.0",
  "private": true,
  "scripts": {
    "bundle-python": "./scripts/bundle-python.sh",
    "dev": "cargo tauri dev",
    "build": "npm run bundle-python && cargo tauri build",
    "build:skip-bundle": "cargo tauri build",
    "tauri": "cargo tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
